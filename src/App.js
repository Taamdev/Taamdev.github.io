import * as React from 'react';
import { BrowserRouter } from 'react-router-dom';
import Base from './Base';

function App() {
  return (
    <BrowserRouter>
      <Base />
    </BrowserRouter>
  );
}

export default App;
